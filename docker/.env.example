# Credenciales de la base de datos (mismas que usa app-backend/.env.example).
# Copiar a ".env" en esta misma carpeta antes de levantar:
#   docker compose -f docker/compose.yml up -d
POSTGRES_USER=ventas_user
POSTGRES_PASSWORD=ventas_password
POSTGRES_DB=ventas_forecast
